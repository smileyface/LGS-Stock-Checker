# This file specifies the development and testing dependencies.
# It includes all production dependencies from requirements.txt
# and adds packages needed only for testing and development.

-r requirements.txt

# Testing-specific packages
pytest==8.3.4
pytest-cov==5.0.0
pytest-mock==3.14.0
