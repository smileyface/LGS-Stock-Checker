{
    "name": "lgs-stock-checker",
    "version": "1.0.0",
    "description": "Runs frontend and backend servers concurrently.",
    "scripts": {
        "dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build",
        "stop": "docker compose down"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "dependencies": {
        "ajv": "^8.17.1"
    }
}
